<UserControl x:Class="PP.Wpf.Demo.Views.AutoGridCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:pp="https://www.cnblogs.com/pumbaa"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource PP.Brushes.Background}" Foreground="{DynamicResource PP.Brushes.Foreground}">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5">
            <TextBlock VerticalAlignment="Center">数据量：</TextBlock>
            <TextBox x:Name="input" Width="200"></TextBox>
            <Button x:Name="btn" Margin="6 0 0 0">确定</Button>
        </StackPanel>
        <pp:AutoGridCanvas x:Name="agc" Margin="5" Background="LightGray" StrokeThickness="1" Stroke="Black">
            <pp:AutoGridCanvas.ItemTemplate>
                <DataTemplate>
                    <Border Background="LightCyan">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="36" Text="{Binding}" />
                    </Border>
                </DataTemplate>
            </pp:AutoGridCanvas.ItemTemplate>
        </pp:AutoGridCanvas>
    </DockPanel>
</UserControl>
